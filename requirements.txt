# Core dependencies
fastapi>=0.104.0,<1.0
uvicorn>=0.24.0,<1.0
jinja2>=3.1.0,<4.0
python-dotenv>=1.0.0,<2.0

# Vector database & embedding
qdrant-client>=2.7.0,<3.0
fastembed>=0.2.0,<1.0

# Hybrid retrieval: BM25 sparse search (core/hybrid_search.py).
# Missing this silently degrades every retrieve_*_hybrid() to dense-only.
rank-bm25>=0.2.2,<1.0

# Knowledge base ingestion
beautifulsoup4>=4.12.0,<5.0
requests>=2.31.0,<3.0
# PDF parsing for the IEC 62443 knowledge base (iec_kb/fetch_iec.py).
# Only needed when (re)building that index from your own licensed PDFs.
pdfplumber>=0.10.0,<1.0

# LLM integration
anthropic>=0.40.0,<1.0

# Report generation
markdown>=3.5.0,<4.0
weasyprint>=60.0,<61.0

# Optional: async performance
uvloop>=0.19.0,<1.0; sys_platform != 'win32'

# Development & testing
pytest>=7.4.0,<8.0; extra == "dev"
